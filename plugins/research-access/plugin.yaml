name: research-access
license: MIT
version: 1.0.0
description: Free NCBI scholarly extraction and local Firecrawl; optionally replaces the firecrawl provider at startup.
kind: backend
provides_web_providers:
  - research-access
provides_hooks:
  - pre_tool_call
provides_tools:
  - browser_snapshot
